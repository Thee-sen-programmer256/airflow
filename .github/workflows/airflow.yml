name: Airflow_CI
on:
  push:
     branches: [data]
  pull_request:
     branches: [data]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: List all files
        run: ls

      - name: Install Python
        uses: actions/checkout@v2
        with:
           python-version: '3.10' 
      - run: python .env_/run_ci_date.py


      - name: List all files
        run: ls


        

